{
  "name": "cogumelos",
  "private": true,
  "scripts": {
    "dev": "live-server --port=4200",
    "build": "node scripts/build.js",
    "extract:texts": "node scripts/extract-texts.js",
    "clean:translations": "node scripts/remove-stale-translations.js"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "prettier": "^3.9.6"
  }
}
